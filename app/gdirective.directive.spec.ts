import { GdirectiveDirective } from './gdirective.directive';

describe('GdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new GdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
